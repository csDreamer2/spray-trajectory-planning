# 自动喷涂轨迹规划系统设计文档

## 概述

自动喷涂轨迹规划系统是一个集成的工业自动化解决方案，专为大型复杂工件的喷涂作业而设计。系统采用模块化架构，包含工件放置推荐子系统、基准面对齐子系统、轨迹规划子系统、喷涂控制子系统、仿真子系统和多工件管理子系统六个核心模块。

系统工作流程分为两个阶段：
1. **规划阶段**：导入工件数据，智能分析并推荐最佳放置位置，支持单工件和多工件布局优化
2. **执行阶段**：工件实际放置后，通过基准面扫描或外部扫描系统实现精确对齐，然后生成优化的喷涂轨迹

系统支持处理外缸上/下半、排缸上/下半、轴承座、阀壳等多种工件类型，工件尺寸范围从1.5m×1.0m×1.0m到6.2m×3.7m×1.8m。系统要求在5分钟内完成对齐，30分钟内完成轨迹规划，达到95%以上的覆盖率，并支持环氧富锌漆和有机硅耐高温漆两种工艺。

**多工件喷涂模式**：系统支持5类工件的分类管理，通过外部扫描系统获取精确位置数据，实现批量工件的智能布局、碰撞检测和优化轨迹规划，显著提高生产效率和设备利用率。

## 架构

系统采用分层模块化架构，分为表示层、业务逻辑层、数据访问层和硬件接口层：

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                                    表示层                                                │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │ 放置推荐UI  │ │ 对齐校正UI  │ │ 轨迹规划UI  │ │VTK 3D仿真UI │ │ 多工件管理UI│       │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘       │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                  业务逻辑层                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │放置推荐模块 │ │ 对齐校正模块│ │ 轨迹规划模块│ │ 仿真计算模块│ │ 多工件管理  │       │
│  │             │ │             │ │             │ │             │ │             │       │
│  │ 几何分析    │ │ 基准面扫描  │ │ 点云处理    │ │VTK渲染引擎  │ │ 批次管理    │       │
│  │ 位置优化    │ │ 特征匹配    │ │ 区域划分    │ │ 碰撞检测    │ │ 布局分析    │       │
│  │ 布局规划    │ │ 坐标变换    │ │ 路径生成    │ │ 轨迹可视化  │ │ 类别管理    │       │
│  │ 多工件优化  │ │ 外部数据接收│ │ 批量轨迹    │ │ 批量仿真    │ │ 碰撞检测    │       │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘       │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                  数据访问层                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │ CAD数据管理 │ │ 扫描数据管理│ │ 轨迹数据管理│ │ 配置管理    │ │ 批次数据管理│       │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘       │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                  硬件接口层                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │ 扫描仪接口  │ │ 机器人接口  │ │ PLC通信接口 │ │ 传感器接口  │ │外部扫描接口 │       │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘       │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

## 组件和接口

### 工件放置推荐子系统

**核心组件:**
- **几何分析引擎**: 分析工件CAD数据，提取关键几何特征
- **位置优化算法**: 基于工件特征和设备约束计算最佳放置位置
- **布局规划器**: 支持单工件和多工件的整体布局优化
- **可达性分析器**: 评估机器人对工件各部位的可达性

**主要接口:**
```typescript
interface IPlacementRecommendationEngine {
  analyzeWorkpiece(workpieceData: WorkpieceCAD): Promise<WorkpieceAnalysis>
  recommendSinglePlacement(analysis: WorkpieceAnalysis): Promise<PlacementRecommendation>
  recommendMultiPlacement(analyses: WorkpieceAnalysis[]): Promise<MultiPlacementLayout>
  validatePlacement(placement: PlacementRecommendation): Promise<ValidationResult>
}
```

### 基准面对齐子系统

**核心组件:**
- **基准面扫描器**: 快速扫描关键基准面和特征点
- **特征匹配算法**: 将扫描数据与CAD模型进行精确匹配
- **坐标变换计算器**: 计算实际位置与理论位置的变换矩阵
- **对齐精度验证器**: 验证对齐结果是否满足精度要求

**主要接口:**
```typescript
interface IAlignmentEngine {
  scanReferencePlanes(scanRegions: ScanRegion[]): Promise<ScanResult>
  matchFeatures(scanData: ScanResult, cadModel: WorkpieceCAD): Promise<FeatureMatchResult>
  calculateAlignment(matchResult: FeatureMatchResult): Promise<AlignmentTransform>
  validateAlignment(transform: AlignmentTransform): Promise<AlignmentValidation>
}
```

### 轨迹规划子系统

**核心组件:**
- **点云处理引擎**: 负责PLY、OBJ、PCD等格式的解析，执行去噪、滤波、轻量化处理
- **区域划分引擎**: 支持手动、自动、混合三种模式的区域划分
- **路径生成算法**: 基于几何特征和工艺参数生成优化轨迹
- **工艺参数管理器**: 管理不同材料和工件的工艺模板

**主要接口:**
```typescript
interface ITrajectoryPlanner {
  loadPointCloud(filePath: string, format: PointCloudFormat): Promise<PointCloud>
  processPointCloud(cloud: PointCloud): Promise<ProcessedPointCloud>
  divideRegions(cloud: ProcessedPointCloud, mode: DivisionMode): Promise<Region[]>
  generateTrajectory(regions: Region[], parameters: SprayParameters): Promise<Trajectory>
  exportRobotProgram(trajectory: Trajectory, format: RobotProgramFormat): Promise<string>
}
```

### 喷涂控制子系统

**核心组件:**
- **通信管理器**: 管理与安川机器人的MotoPlus TCP通信
- **参数控制器**: 实时调整喷涂压力、流量、速度等参数
- **安全监控器**: 监控系统状态，处理异常和安全事件
- **断点续喷管理器**: 支持任务中断和恢复功能

**主要接口:**
```typescript
interface ISprayController {
  connectRobot(robotConfig: RobotConfig): Promise<boolean>
  executeTrajectory(trajectory: Trajectory): Promise<ExecutionResult>
  monitorExecution(): Observable<ExecutionStatus>
  handleEmergencyStop(): Promise<void>
  resumeFromBreakpoint(breakpoint: Breakpoint): Promise<void>
}
```

### 喷涂仿真子系统

**核心组件:**
- **3D渲染引擎**: 基于VTK的专业科学可视化渲染
- **碰撞检测算法**: 实时检测轨迹与环境的碰撞
- **质量预测模型**: 预测膜厚分布和覆盖率
- **统计分析器**: 计算喷涂时间、涂料消耗等指标

**主要接口:**
```typescript
interface ISimulationEngine {
  loadEnvironment(environment: Environment3D): Promise<void>
  simulateTrajectory(trajectory: Trajectory): Promise<SimulationResult>
  detectCollisions(trajectory: Trajectory): Promise<Collision[]>
  predictQuality(trajectory: Trajectory, parameters: SprayParameters): Promise<QualityPrediction>
}
```

### 多工件管理子系统

**核心组件:**
- **多工件管理器**: 管理多工件批次的创建、验证和状态跟踪
- **工件类别管理器**: 管理5类工件的分类定义和参数配置
- **布局分析引擎**: 分析多工件摆放的合理性和空间利用率
- **碰撞检测引擎**: 检测工件间和轨迹间的潜在碰撞风险
- **外部数据接收器**: 接收外部扫描系统提供的工件位置数据
- **批量轨迹规划器**: 为多工件批次生成优化的喷涂轨迹

**主要接口:**
```typescript
interface IMultiWorkpieceManager {
  initialize(categories: WorkpieceCategory[]): Promise<void>
  createNewBatch(): string
  receiveWorkpieceData(data: ScannedWorkpieceData): Promise<void>
  receiveBatchData(workpieces: ScannedWorkpieceData[]): Promise<void>
  validateCurrentBatch(): Promise<ValidationResult>
  analyzeLayout(): LayoutAnalysis | null
  getBatchStatistics(): BatchStatistics | null
}

interface IBatchTrajectoryPlanner {
  planBatchTrajectory(workpieces: ScannedWorkpieceData[]): Promise<BatchTrajectory>
  optimizeSpraySequence(trajectory: BatchTrajectory): Promise<OptimizedTrajectory>
  detectBatchCollisions(trajectory: BatchTrajectory): Promise<CollisionReport>
  generateBatchExecutionPlan(trajectory: OptimizedTrajectory): Promise<BatchExecutionPlan>
}
```

## 数据模型

### 核心数据结构

```typescript
// 工件CAD数据模型
interface WorkpieceCAD {
  id: string
  name: string
  geometry: CADGeometry
  features: GeometricFeature[]
  dimensions: Dimensions3D
  mass: number
  centerOfMass: Point3D
  referencePlanes: ReferencePlane[]
  createdAt: Date
}

// 放置推荐模型
interface PlacementRecommendation {
  id: string
  workpieceId: string
  position: Point3D
  orientation: Vector3D
  distanceToReferencePlanes: {
    [planeName: string]: number
  }
  reachabilityScore: number
  collisionRisk: number
  confidence: number
  reasoning: string[]
}

// 基准面扫描结果
interface ScanResult {
  id: string
  scanRegions: ScanRegion[]
  detectedFeatures: DetectedFeature[]
  scanQuality: number
  timestamp: Date
}

// 对齐变换矩阵
interface AlignmentTransform {
  translation: Vector3D
  rotation: RotationMatrix
  scale: number
  accuracy: number
  residualError: number
}

// 点云数据模型
interface PointCloud {
  points: Point3D[]
  normals?: Vector3D[]
  colors?: Color[]
  metadata: PointCloudMetadata
}

// 轨迹数据模型
interface Trajectory {
  id: string
  workpieceId: string
  segments: TrajectorySegment[]
  parameters: SprayParameters
  estimatedTime: number
  estimatedMaterial: number
}

// 喷涂参数模型
interface SprayParameters {
  pressure: number        // 喷涂压力 (bar)
  flowRate: number       // 流量 (ml/min)
  sprayHeight: number    // 喷涂高度 (mm)
  spraySpeed: number     // 喷涂速度 (mm/s)
  fanWidth: number       // 扇面宽度 (mm)
  overlapRate: number    // 重叠率 (%)
  materialType: MaterialType
}

// 工件模型
interface Workpiece {
  id: string
  type: WorkpieceType    // 外缸上半、外缸下半、排缸上半等
  dimensions: Dimensions3D
  material: string
  coatingRequirements: CoatingRequirements
}

// 涂层要求模型
interface CoatingRequirements {
  materialType: MaterialType  // 环氧富锌漆、有机硅耐高温漆
  targetThickness: number     // 目标膜厚 (μm)
  toleranceRange: {           // 允差范围
    min: number              // 80% of target
    max: number              // 150% of target
  }
  coverageRate: number        // 要求覆盖率 (%)
}

// 扫描工件数据模型
interface ScannedWorkpieceData {
  workpieceId: string       // 工件唯一标识
  categoryId: string        // 工件类别ID
  pointCloud: PointCloud    // 扫描点云数据
  position: Point3D         // 精确位置坐标
  orientation: Vector3D     // 姿态向量
  confidence: number        // 识别置信度 (0-1)
  scanTimestamp: Date       // 扫描时间戳
  boundingBox?: {           // 工件包围盒
    min: Point3D
    max: Point3D
  }
}

// 工件类别定义
interface WorkpieceCategory {
  categoryId: string
  name: string
  type: 'large_single' | 'small_batch'
  dimensions: {
    length: number
    width: number
    height: number
  }
  maxBatchSize: number      // 最大批量数量
  minSpacing: number        // 最小间距要求 (mm)
  sprayParameters: SprayParameters
}

// 平板车规格
interface CartSpecification {
  length: number            // 长度 (mm)
  width: number             // 宽度 (mm)
  height: number            // 高度 (mm)
  loadCapacity: number      // 载重 (kg)
  workArea: {               // 有效工作区域
    x: { min: number; max: number }
    y: { min: number; max: number }
    z: { min: number; max: number }
  }
}

// 批次信息
interface BatchInfo {
  batchId: string
  status: BatchStatus
  workpieces: ScannedWorkpieceData[]
  categories: Set<string>
  createdAt: Date
  updatedAt: Date
  validation?: ValidationResult
  layout?: LayoutAnalysis
}

// 布局分析结果
interface LayoutAnalysis {
  totalWorkpieces: number
  spaceUtilization: number    // 空间利用率 (0-1)
  collisionRisks: CollisionRisk[]
  accessibilityIssues: AccessibilityIssue[]
  recommendations: string[]
}

// 批量轨迹模型
interface BatchTrajectory {
  batchId: string
  workpieceTrajectories: WorkpieceTrajectory[]
  spraySequence: SpraySequenceItem[]
  totalEstimatedTime: number
  totalEstimatedMaterial: number
  collisionFreeGuarantee: boolean
}

// 工件轨迹
interface WorkpieceTrajectory {
  workpieceId: string
  trajectory: Trajectory
  sprayOrder: number
  dependencies: string[]     // 依赖的其他工件ID
}

// 喷涂顺序项
interface SpraySequenceItem {
  workpieceId: string
  startTime: number
  endTime: number
  transitionPath?: Trajectory  // 到下一个工件的过渡路径
}
```

## 正确性属性

*属性是一个特征或行为，应该在系统的所有有效执行中保持为真——本质上，是关于系统应该做什么的正式声明。属性作为人类可读规范和机器可验证正确性保证之间的桥梁。*

基于前期分析，以下是系统的核心正确性属性：

### 属性 1: 点云格式解析一致性
*对于任何*有效的PLY、OBJ或PCD格式文件，解析后重新导出应该产生等价的几何数据
**验证: 需求 1.1**

### 属性 2: 轨迹规划性能保证
*对于任何*输入的工件点云数据，轨迹规划时间应该不超过30分钟且覆盖率应该达到95%以上
**验证: 需求 1.3**

### 属性 3: 参数配置往返一致性
*对于任何*保存的工艺参数模板，加载后的参数值应该与保存前完全一致
**验证: 需求 2.4**

### 属性 4: 仿真时间性能约束
*对于任何*有效的轨迹数据，仿真计算时间应该不超过30分钟
**验证: 需求 3.1**

### 属性 5: 碰撞检测可靠性
*对于任何*存在碰撞风险的轨迹，系统应该能够检测并暂停执行
**验证: 需求 3.4**

### 属性 6: 权限控制有效性
*对于任何*用户角色，只能访问其权限范围内的功能，超出权限的访问应该被拒绝
**验证: 需求 4.5**

### 属性 7: 通信实时性保证
*对于任何*与机器人的数据交换，延迟应该保持在毫秒级别
**验证: 需求 5.1**

### 属性 8: 异常恢复完整性
*对于任何*系统异常，应该记录完整的故障信息并支持从中断点恢复
**验证: 需求 5.4**

### 属性 9: 数据存储往返一致性
*对于任何*保存到数据库的轨迹和工艺数据，检索后应该与原始数据完全一致
**验证: 需求 6.1**

### 属性 10: 坐标变换保持性
*对于任何*工件位置变化，调整后的轨迹应该保持相对于工件的几何关系不变
**验证: 需求 6.3**

### 属性 11: 膜厚参数约束
*对于任何*喷涂参数计算，结果膜厚应该在目标值的80%-150%范围内
**验证: 需求 7.3**

### 属性 12: 安全响应及时性
*对于任何*安全事件（急停、安全门等），系统应该在安全时间内停止所有运动
**验证: 需求 9.1, 9.2**

### 属性 13: 放置位置推荐准确性
*对于任何*工件CAD数据，推荐的放置位置应该满足可达性和安全性要求
**验证: 需求 1.2, 1.3**

### 属性 14: 对齐精度保证
*对于任何*基准面扫描结果，对齐后的位置偏差应该小于2mm，角度偏差小于1度
**验证: 需求 8.5**

### 属性 15: 对齐时间性能
*对于任何*扫描数据，对齐计算应该在5分钟内完成
**验证: 需求 8.4**

### 属性 16: 多工件数据接收完整性
*对于任何*外部扫描系统提供的工件数据，系统应该验证数据完整性并检查置信度阈值
**验证: 需求 1B.2**

### 属性 17: 批次验证时间性能
*对于任何*多工件批次，布局验证和碰撞检测应该在5分钟内完成
**验证: 需求 1B.4**

### 属性 18: 工件间距安全保证
*对于任何*多工件布局，工件间距应该满足最小安全距离要求
**验证: 需求 1B.5**

### 属性 19: 批量轨迹无碰撞保证
*对于任何*批量轨迹规划，生成的路径应该确保无碰撞执行
**验证: 需求 10.4**

### 属性 20: 多工件连续作业完整性
*对于任何*批量喷涂任务，应该连续完成所有工件而无需中途移动工件
**验证: 需求 10.5**

## 错误处理

### 错误分类和处理策略

**1. 输入数据错误**
- 文件格式不支持: 返回明确错误信息，建议支持的格式
- 点云数据损坏: 尝试自动修复，修复失败则提示用户
- 参数超出范围: 自动调整到有效范围并警告用户

**2. 系统运行错误**
- 内存不足: 自动释放缓存，启用数据分块处理
- 计算超时: 提供简化算法选项，降低精度换取速度
- 网络通信中断: 自动重连机制，超时后进入离线模式

**3. 硬件接口错误**
- 机器人连接失败: 重试连接，记录错误日志
- 传感器数据异常: 使用备用传感器或估算值
- PLC通信故障: 切换到安全模式，停止危险操作

**4. 安全相关错误**
- 碰撞检测: 立即停止运动，等待人工确认
- 急停触发: 安全停机，记录事件，要求手动复位
- 权限违规: 拒绝操作，记录安全日志

## 测试策略

### 单元测试策略

**测试范围:**
- 点云处理算法的正确性
- 轨迹生成算法的有效性
- 参数验证和转换函数
- 数据库操作的完整性
- 通信协议的实现

**测试工具:** Jest + TypeScript，覆盖率目标 > 80%

### 属性基础测试策略

**测试框架:** 使用fast-check进行属性基础测试，每个属性测试运行最少100次迭代

**测试生成器设计:**
- 点云数据生成器: 生成不同复杂度和噪声水平的点云
- 轨迹参数生成器: 生成有效范围内的随机参数组合
- 工件几何生成器: 生成符合实际尺寸约束的工件模型
- 用户权限生成器: 生成不同角色和权限组合

**属性测试标记格式:**
每个属性测试必须使用以下格式标记：
`**Feature: spray-trajectory-planning, Property {number}: {property_text}**`

### 集成测试策略

**测试场景:**
- 完整的轨迹规划流程测试
- 机器人通信集成测试
- 数据库集成测试
- 用户界面集成测试

**测试环境:** 使用Docker容器化测试环境，包含模拟的机器人和PLC

### 性能测试策略

**关键指标:**
- 轨迹规划时间 < 30分钟
- 仿真计算时间 < 30分钟
- 通信延迟 < 10ms
- 系统响应时间 < 2秒

**测试工具:** 使用Artillery进行负载测试，JMeter进行性能基准测试

## 项目当前状态 (2024年12月)

### ✅ 已完成功能
1. **基础UI框架**
   - Qt6主窗口和响应式布局
   - 菜单栏、工具栏、状态栏
   - 停靠窗口和面板系统

2. **点云数据处理**
   - PLY格式点云文件解析
   - 异步加载机制
   - 内存优化和性能处理

3. **VTK 3D可视化引擎**
   - VTK 9.2专业渲染管线
   - 点云显示和交互控制
   - 轨迹可视化和场景管理
   - 相机控制和视图操作

4. **批量管理系统**
   - 工件批次管理
   - 状态跟踪和数据持久化

### ❌ 已弃用方案
1. **Unity 3D集成**
   - 原因：窗口嵌入复杂度高，UI交互问题
   - 替代：VTK提供更好的Qt原生集成

### 🔄 开发中功能
1. **STEP/CAD模型支持**
   - 需要集成OpenCASCADE
   - 支持车间模型和复杂几何体

### ⏳ 待开发功能
1. **轨迹规划算法**
2. **机器人通信协议**
3. **碰撞检测系统**
4. **仿真验证模块**

### 🏗️ 技术架构决策
- **3D引擎**: VTK (替代Unity) - 专业、稳定、Qt原生
- **依赖管理**: 用户编译VTK + 项目内vcpkg
- **构建系统**: CMake + MSVC 2022
- **UI框架**: Qt6 + 现代C++17

### 📊 性能对比 (Unity vs VTK)
| 指标 | Unity | VTK | 改进 |
|------|-------|-----|------|
| 启动时间 | 5-8秒 | 1-2秒 | ✅ 75% |
| 内存占用 | 200MB+ | 50MB | ✅ 75% |
| 集成复杂度 | 高 | 低 | ✅ |
| 3D专业性 | 游戏级 | 工业级 | ✅ |
| Qt兼容性 | 差 | 完美 | ✅ |